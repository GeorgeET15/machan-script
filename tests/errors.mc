Machane!!

machane try cheyu {
  para("Inside try block...");
  // Attempt to use a variable that doesn't exist
  para("Mystery: ", mystery_var);
} pidiku(error) {
  para("Caught error: ", error);
}

para("Execution continues safely.");
